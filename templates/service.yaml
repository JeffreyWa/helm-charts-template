apiVersion: v1
kind: Service
metadata:
  name: {{ include "testtbs.fullname" . }}
  labels:
{{ include "testtbs.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "testtbs.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
