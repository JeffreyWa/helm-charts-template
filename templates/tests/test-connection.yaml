apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testtbs.fullname" . }}-test-connection"
  labels:
{{ include "testtbs.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "testtbs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
