apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chatserver.fullname" . }}-test-connection"
  labels:
{{ include "chatserver.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "chatserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
